# Uses the free Google Gemini API
- version 1.12.1

# Activate the virtual environment

`source .venv/bin/activate`

# Example Command
`uv run main.py "execute the calulator app's tests (tests.py)" --verbose`